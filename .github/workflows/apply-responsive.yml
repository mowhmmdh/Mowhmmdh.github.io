name: Legacy responsive layer (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  responsive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check unified responsive system
        run: |
          test -f assets/stable-ui-2026.css
          echo "PASS: stable-ui-2026.css is the active responsive foundation"