(()=>{const root=document.documentElement,KEY='personal-theme',preferred=matchMedia('(prefers-color-scheme:light)').matches?'light':'dark';const path=location.pathname.toLowerCase();const page=path==='/ '||path==='/'?'home':path.includes('about')?'about':path.includes('projects')?'projects':path.includes('services')?'services':path.includes('/blog')||path.includes('en-blog')?'blog':path.includes('vintech')?(path.includes('/vintech/')||path.includes('/en-vintech/')?'service':'vintech'):'site';const apply=theme=>{root.dataset.theme=theme;root.style.colorScheme=theme;localStorage.setItem(KEY,theme);document.querySelectorAll('.theme-toggle,.vt-theme-toggle').forEach(b=>{b.textContent=theme==='dark'?'☀':'☾';b.setAttribute('aria-label',theme==='dark'?'Switch to light mode':'Switch to dark mode');b.title=theme==='dark'?'Light mode':'Dark mode'})};const init=()=>{document.body.classList.add('personal-site','page-'+page);document.body.dataset.page=page;document.querySelectorAll('.theme-toggle,.vt-theme-toggle').forEach(b=>{b.onclick=()=>apply(root.dataset.theme==='dark'?'light':'dark')});document.querySelectorAll('.mobile-menu').forEach(btn=>{const nav=btn.closest('.site-nav')||document.querySelector('.site-nav');const links=nav?.querySelector('.nav-links');if(!nav||!links)return;let panel=nav.querySelector('.mobile-panel');if(!panel){panel=document.createElement('div');panel.className='mobile-panel';panel.innerHTML=links.innerHTML;nav.appendChild(panel)}btn.onclick=()=>{const open=document.body.classList.toggle('menu-open');btn.setAttribute('aria-expanded',String(open))};panel.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>document.body.classList.remove('menu-open')))});let ticking=false;const progress=()=>{const h=document.documentElement.scrollHeight-innerHeight;root.style.setProperty('--scroll-progress',h>0?(scrollY/h).toFixed(4):'0');ticking=false};addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(progress);ticking=true}},{passive:true});progress();document.querySelectorAll('.card,.service-card,.project-card,.post-card,.metric,.stat,.vt-card').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--card-x',(e.clientX-r.left)+'px');card.style.setProperty('--card-y',(e.clientY-r.top)+'px')}));apply(localStorage.getItem(KEY)||preferred)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();