(()=>{
  const root=document.documentElement;
  const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const isVinTech=()=>!!document.querySelector('.vt-nav');
  const injectRecovery=()=>{if(document.getElementById('vt-visual-recovery'))return;const l=document.createElement('link');l.id='vt-visual-recovery';l.rel='stylesheet';l.href='/assets/visual-recovery-2026.css';document.head.appendChild(l)};
  const initTheme=()=>{if(!isVinTech())return;const key='vintech-theme',saved=localStorage.getItem(key),theme=saved==='light'||saved==='dark'?saved:(matchMedia('(prefers-color-scheme:light)').matches?'light':'dark');root.dataset.theme=theme;root.classList.toggle('light',theme==='light');let toggle=document.querySelector('.vt-theme-toggle');if(!toggle){toggle=document.createElement('button');toggle.className='vt-theme-toggle';toggle.type='button';toggle.innerHTML='<span class="vt-theme-sun" aria-hidden="true">☀</span><span class="vt-theme-moon" aria-hidden="true">☾</span>';const tools=document.querySelector('.vt-nav-tools'),menu=document.querySelector('.vt-menu');if(tools)tools.insertBefore(toggle,menu||null)}const fa=(root.lang||'').toLowerCase().startsWith('fa'),sync=()=>{const light=root.dataset.theme==='light';toggle.setAttribute('aria-label',fa?(light?'تغییر به حالت تاریک':'تغییر به حالت روشن'):(light?'Switch to dark mode':'Switch to light mode'));toggle.title=fa?(light?'حالت تاریک':'حالت روشن'):(light?'Dark mode':'Light mode');toggle.setAttribute('aria-pressed',String(light))};sync();toggle.onclick=()=>{const next=root.dataset.theme==='light'?'dark':'light';root.dataset.theme=next;root.classList.toggle('light',next==='light');localStorage.setItem(key,next);sync()}};
  const initNav=()=>{if(!isVinTech())return;const header=document.querySelector('.vt-nav'),menu=document.querySelector('.vt-menu'),nav=document.querySelector('.vt-links');if(!header||!menu||!nav)return;const close=()=>{menu.setAttribute('aria-expanded','false');header.classList.remove('is-open');document.body.classList.remove('vt-menu-open')},open=()=>{menu.setAttribute('aria-expanded','true');header.classList.add('is-open');document.body.classList.add('vt-menu-open')};menu.setAttribute('aria-expanded','false');menu.addEventListener('click',()=>menu.getAttribute('aria-expanded')==='true'?close():open());nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})};
  const initCards=()=>{document.querySelectorAll('.card,.service-card,.project-card,.timeline-item,.skill-category,.faq-item,.portrait,.profile-card,.cta,.metric,.vt-card').forEach(el=>{if(reduce)return;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--card-x',`${e.clientX-r.left}px`);el.style.setProperty('--card-y',`${e.clientY-r.top}px`)},{passive:true})})};
  const initReveal=()=>{if(!isVinTech()||reduce||!('IntersectionObserver'in window))return;const items=document.querySelectorAll('[data-reveal]');items.forEach(el=>el.classList.add('vt-ready'));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});items.forEach(el=>io.observe(el))};
  const init=()=>{injectRecovery();initTheme();initNav();initCards();initReveal()};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
